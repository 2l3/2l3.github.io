
@model Hospital.Models.WebsiteData
@using Hospital.Resources;

@{
    ViewBag.Title = Resource.WebsiteData;
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<div class="content-wrapper">
    @using (Html.BeginForm("SaveWebsiteData", "ReportSetting", FormMethod.Post, new { enctype = "multipart/form-data" }))
    {
        @Html.AntiForgeryToken()
        @Html.HiddenFor(s => s.Id)
        @Html.HiddenFor(s => s.CreatedBy)
        @Html.HiddenFor(s => s.CreatedDate)
        @Html.HiddenFor(s => s.CreatorName)
        <p style="text-align:center;color:red">@ViewBag.Message</p>

        <div class="form-horizontal">
            <hr />
            @Html.ValidationSummary(true)
            <div class="form-group">
                <div class="control-label col-md-2">
                    @Resource.PPMStickerText :
                </div>
                <div class="col-md-8">
                    @Html.EditorFor(model => model.PPMStickerText, new { htmlAttributes = new { @class = "form-control", @required = "required" } })
                    @Html.ValidationMessageFor(model => model.PPMStickerText, "", new { @class = "text-danger" })
                </div>
            </div>
            <div class="form-group">
                <div class="control-label col-md-2">
                    @Resource.Password :
                </div>
                <div class="col-md-8">
                    @Html.EditorFor(model => model.Password, new { htmlAttributes = new { @class = "form-control" } })
                    @Html.ValidationMessageFor(model => model.Password, "", new { @class = "text-danger" })
                </div>
            </div>
            <div class="form-group">
                @Html.Label(Resource.Image, htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="col-md-8">
                    <div class="input-group image-preview">
                        <span class="input-group-btn">
                            <!-- image-preview-input -->
                            <div class="btn btn-default image-preview-input" style="width:100%;">
                                <input type="file" id="Img" accept="image/png, image/jpeg, image/jpg" name="Img"  /> <!-- rename it -->
                            </div>
                        </span>

                    </div>
                </div>
            </div>
            <div class="form-group">
                <div class="col-md-offset-2 col-md-10">
                    <input type="submit" value="@Resource.Save" class="btn btn-primary" name="Save" />
                </div>
            </div>
        </div>
    }


</div>

<script type="text/javascript" src="~/Content2/Ar-assets/js/lib/jquery-1.11.min.js"></script>

<script>


    $(function () {
        $('form').submit(function () {
            $('input[name="Save"]').prop('disabled', true);
        });
    });

</script>